# Store client credentials of AASX Server
#
# Use comment lines starting with # as needed
# Comma separated columns are:
# URL-Prefix, AuthType, Para1, Para2, .. ParaN
# URL-Prefix = For each client HTTP operation the URL prefix will be compared and the corresponding authentication will be made
# AuthType = email | userpw | x509 | secret
# Para1 to ParaN: Parameters as needed for AuthType
# Parameters for email: para1 = email (passed by query parameter ?Email=)
# Parameters for basicauth: para1 = username, para2 = Password (passed by HTTP header)
# Parameters for bearer: Para1 = endpoint, Para2 = ClientCertfile, Para3 = Password (if needed)(uses OpenId server admin-shell-io.com/50001)
# Parameters for bearer: Para1 = endpoint, Para2 = Client ID, Para3 = Client Secret
# Parameters for entraid: Para1 = endpoint
# Parameters for secret: Para1 = secret (passed by query parameter ?s=)
#
# URL-Prefix, bearer, endpoint, ClientCertfile, passsword for this URL-Prefix
# https://plugfest4.,bearer,https://auth.aas-voyager.com,Andreas_Orzelski_Chain.pfx,i40
https://plugfest4.,entraid,https://auth.aas-voyager.com
https://plugfest5.,bearer,https://credential.aas-voyager.com/token,aorzelski@phoenixcontact.com,aorzelski@phoenixcontact.com-secret
https://plugfest6.,bearer,https://admin-shell-io.com/50001,Andreas_Orzelski_Chain.pfx,i40
